ALGORITHM word
VAR
  str:String:="hello word";
  count:Integer:=1;
BEGIN
    FOR i in str DO
        IF (str.charAt[i]=='') THEN
            count+=1;
        END_IF
    END_FOR
     write(count);
END